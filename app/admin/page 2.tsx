"use client";

import { useState } from "react";
import { BarChart3, CalendarDays, Check, ChevronRight, Download, FileVideo, LayoutDashboard, Menu, Plus, Settings, Shield, Upload, Users, X } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle, DialogTrigger } from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";

type Session = { name: string; dob: string; focus: string; date: string; question: string; goal: string };
const sessions: Session[] = [{ name: "Aarav Mehta", dob: "14 May 1992", focus: "Career direction", date: "Aug 12 · 11:00 AM", question: "I have been offered a leadership role at a fast-growing company, but it means leaving a stable team I have built over five years. What does my current cycle suggest about making this transition?", goal: "Choose the career path that supports long-term growth without sacrificing financial stability." }, { name: "Mira Shah", dob: "22 Nov 1987", focus: "Relationship", date: "Aug 12 · 2:30 PM", question: "My partner and I are considering moving cities together. I want to understand what our combined numbers say about the timing and whether this change will strengthen our connection.", goal: "Find an aligned timeline for a shared home and a more harmonious partnership." }, { name: "Karan Malhotra", dob: "03 Feb 1995", focus: "Wealth timing", date: "Aug 13 · 10:00 AM", question: "I am launching a second business while my first is still growing. Which months are most supportive for investment and where should I be cautious with expansion?", goal: "Build a practical 12-month wealth plan with a clear risk threshold." }];

function AdminSidebar({ open, close }: { open: boolean; close: () => void }) { const links = [[LayoutDashboard, "Command center"], [CalendarDays, "Sessions"], [FileVideo, "Content manager"], [Users, "Users"]]; return <aside className={`fixed inset-y-0 left-0 z-40 w-64 border-r border-white/10 bg-[#0b0d19] p-5 transition-transform lg:static lg:translate-x-0 ${open ? "translate-x-0" : "-translate-x-full"}`}><div className="flex items-center justify-between px-2 py-3"><span className="font-display text-xl"><span className="text-gold">AURA</span> admin</span><button onClick={close} className="text-lav lg:hidden" aria-label="Close admin navigation"><X className="h-5 w-5" /></button></div><div className="mt-10 space-y-2">{links.map(([Icon, label], index) => <a key={label as string} href={index === 0 ? "#" : `#${(label as string).toLowerCase().replace(" ", "-")}`} onClick={close} className={`flex items-center gap-3 rounded-lg px-3 py-3 text-sm ${index === 0 ? "bg-gold text-midnight" : "text-lav hover:bg-white/5 hover:text-cream"}`}><Icon className="h-4 w-4" />{label as string}</a>)}</div><div className="mt-8 border-t border-white/10 pt-5"><a href="#settings" className="flex items-center gap-3 px-3 py-3 text-sm text-lav hover:text-cream"><Settings className="h-4 w-4" />Settings</a></div><div className="mt-auto hidden rounded-xl border border-gold/15 bg-gold/5 p-3 lg:block"><Shield className="h-5 w-5 text-gold" /><p className="mt-3 text-xs font-medium text-cream">Private workspace</p><p className="mt-1 text-[11px] leading-relaxed text-lav">Your client data is visible only to your practice team.</p></div></aside>; }

function ExportButton() { function exportCsv() { const header = "Client Name,Date of Birth,Focus Area,Session Date"; const rows = sessions.map(session => [session.name, session.dob, session.focus, session.date].map(value => `"${value.replaceAll('"', '""')}"`).join(",")); const blob = new Blob([[header, ...rows].join("\n")], { type: "text/csv;charset=utf-8" }); const url = URL.createObjectURL(blob); const link = document.createElement("a"); link.href = url; link.download = "aura-upcoming-sessions.csv"; link.click(); URL.revokeObjectURL(url); } return <Button variant="outline" size="sm" onClick={exportCsv}><Download className="h-4 w-4" />Export to CSV</Button>; }

export default function AdminPage() { const [menuOpen, setMenuOpen] = useState(false); const [premium, setPremium] = useState(true); const [uploaded, setUploaded] = useState(false); return <main className="min-h-screen bg-[#090b16] text-cream"><div className="flex min-h-screen"><AdminSidebar open={menuOpen} close={() => setMenuOpen(false)} />{menuOpen && <button className="fixed inset-0 z-30 bg-midnight/70 lg:hidden" onClick={() => setMenuOpen(false)} aria-label="Close navigation overlay" />}<div className="min-w-0 flex-1"><header className="flex items-center justify-between border-b border-white/10 px-5 py-4 sm:px-8"><button onClick={() => setMenuOpen(true)} className="text-lav lg:hidden" aria-label="Open admin navigation"><Menu className="h-5 w-5" /></button><div className="ml-auto flex items-center gap-3"><span className="hidden text-right sm:block"><span className="block text-sm">Acharya Devraj</span><span className="text-xs text-lav">Administrator</span></span><div className="flex h-9 w-9 items-center justify-center rounded-lg bg-gold font-semibold text-midnight">AD</div></div></header><div className="mx-auto max-w-7xl space-y-8 px-5 py-8 sm:px-8 lg:px-10"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-xs uppercase tracking-[.2em] text-gold">Tuesday · August 11, 2026</p><h1 className="mt-2 font-display text-3xl">Command center</h1><p className="mt-2 text-sm text-lav">A clear view of your practice, today and ahead.</p></div><Button size="sm"><Plus className="h-4 w-4" />New resource</Button></div><div className="grid gap-4 md:grid-cols-3"><Card><CardContent className="p-5"><div className="flex items-start justify-between"><div><p className="text-sm text-lav">Monthly revenue</p><p className="mt-3 text-3xl font-semibold text-cream">₹2,48,750</p><p className="mt-2 text-xs text-emerald-300">↑ 18.4% from last month</p></div><div className="rounded-lg bg-gold/10 p-2 text-gold"><BarChart3 className="h-5 w-5" /></div></div></CardContent></Card><Card><CardContent className="p-5"><div className="flex items-start justify-between"><div><p className="text-sm text-lav">Upcoming sessions</p><p className="mt-3 text-3xl font-semibold">24</p><p className="mt-2 text-xs text-gold">8 sessions this week</p></div><div className="rounded-lg bg-copper/10 p-2 text-copper"><CalendarDays className="h-5 w-5" /></div></div></CardContent></Card><Card><CardContent className="p-5"><div className="flex items-start justify-between"><div><p className="text-sm text-lav">Active users</p><p className="mt-3 text-3xl font-semibold">1,284</p><p className="mt-2 text-xs text-emerald-300">↑ 9.2% this month</p></div><div className="rounded-lg bg-gold/10 p-2 text-gold"><Users className="h-5 w-5" /></div></div></CardContent></Card></div><section id="sessions"><div className="mb-4 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-xs uppercase tracking-[.2em] text-gold">Calendar queue</p><h2 className="mt-2 font-display text-2xl">Upcoming sessions</h2></div><ExportButton /></div><Card><CardContent className="p-2 sm:p-4"><Table><TableHeader><TableRow><TableHead>Client name</TableHead><TableHead>Date of birth</TableHead><TableHead>Focus area</TableHead><TableHead>Session</TableHead><TableHead className="text-right">Actions</TableHead></TableRow></TableHeader><TableBody>{sessions.map(session => <TableRow key={session.name}><TableCell className="font-medium">{session.name}</TableCell><TableCell className="text-lav">{session.dob}</TableCell><TableCell><span className="rounded-full border border-gold/20 bg-gold/5 px-2.5 py-1 text-xs text-goldlite">{session.focus}</span></TableCell><TableCell className="whitespace-nowrap text-lav">{session.date}</TableCell><TableCell className="text-right"><Dialog><DialogTrigger asChild><Button variant="outline" size="sm">View intake <ChevronRight className="h-3.5 w-3.5" /></Button></DialogTrigger><DialogContent><DialogHeader><p className="text-xs uppercase tracking-[.2em] text-gold">Client intake · {session.focus}</p><DialogTitle>{session.name}</DialogTitle><DialogDescription>{session.dob} · Scheduled {session.date}</DialogDescription></DialogHeader><div className="space-y-5"><div><p className="mb-2 text-xs uppercase tracking-wider text-lav">Exact goal</p><p className="rounded-xl border border-gold/15 bg-white/[.03] p-4 text-sm leading-relaxed text-cream">{session.goal}</p></div><div><p className="mb-2 text-xs uppercase tracking-wider text-lav">Submitted question</p><p className="rounded-xl border border-gold/15 bg-white/[.03] p-4 text-sm leading-relaxed text-cream">{session.question}</p></div></div></DialogContent></Dialog></TableCell></TableRow>)}</TableBody></Table></CardContent></Card></section><section id="content-manager"><div className="mb-4"><p className="text-xs uppercase tracking-[.2em] text-gold">Library tools</p><h2 className="mt-2 font-display text-2xl">Content manager</h2></div><Card><CardHeader><CardTitle>Upload a new video resource</CardTitle></CardHeader><CardContent><form className="grid gap-5 md:grid-cols-[1fr_1fr_auto] md:items-end" onSubmit={event => { event.preventDefault(); setUploaded(true); }}><div><Label htmlFor="video-title">Video title</Label><Input id="video-title" required placeholder="e.g. Understanding your Life Path" /></div><div><Label htmlFor="video-url">Video URL</Label><Input id="video-url" required type="url" placeholder="https://..." /></div><div><Label>Access level</Label><button type="button" onClick={() => setPremium(value => !value)} className={`flex h-12 w-full items-center justify-between gap-4 rounded-xl border px-4 text-sm md:min-w-[145px] ${premium ? "border-gold/40 bg-gold/10 text-goldlite" : "border-white/15 bg-white/[.03] text-lav"}`}><span>{premium ? "Premium" : "Free"}</span><span className={`h-5 w-9 rounded-full p-0.5 transition ${premium ? "bg-gold" : "bg-white/20"}`}><span className={`block h-4 w-4 rounded-full bg-midnight transition ${premium ? "translate-x-4" : "translate-x-0"}`} /></span></button></div><Button type="submit" className="md:col-span-3 md:w-fit"><Upload className="h-4 w-4" />Upload resource</Button></form>{uploaded && <p className="mt-4 flex items-center gap-2 text-sm text-emerald-300"><Check className="h-4 w-4" />Resource queued for publishing.</p>}</CardContent></Card></section></div></div></div></main>; }
